#include "../h/util.h"
#include "../h/macros.h"
#include <fstream>


using namespace std;

int main(int argc, char *argv[])
{
       if(argc != 3) {
               PRINT("You entered wrong arguments"); 
               return -1;
       }
       if(!assebler)
}
