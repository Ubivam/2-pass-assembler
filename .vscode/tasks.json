{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Debug",
            "type": "shell",
            "command": "g++ -g -o assembly.exe src/main.cpp",
            "group":"test" 
        },
        {
            "label": "Build",
            "type": "shell",
            "command": "g++ -o assembly.exe src/main.cpp",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}